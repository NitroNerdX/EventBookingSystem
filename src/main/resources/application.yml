spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/EventBookingSystem
    username: postgres
    password: Postgres@00000
  jpa:
    hibernate:
      ddl-auto: validate     # critical — Flyway owns the schema, not Hibernate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true